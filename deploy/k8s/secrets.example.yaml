# Template for the runtime Secret. Copy to secrets.yaml, fill in real
# values, `kubectl apply -f secrets.yaml`. NEVER commit the filled copy —
# deploy/k8s/secrets.yaml is gitignored.
#
# Note: NEXT_PUBLIC_* values must ALSO be passed as --build-arg when
# building the image (they're inlined at build time); they're repeated here
# because server code reads them at runtime too.
apiVersion: v1
kind: Secret
metadata:
  name: press-farm-env
type: Opaque
stringData:
  NEXT_PUBLIC_SUPABASE_URL: https://rxdfjaseilmjvcwamqyk.supabase.co
  NEXT_PUBLIC_SUPABASE_ANON_KEY: REPLACE_ME
  SUPABASE_SERVICE_ROLE_KEY: REPLACE_ME
  RESEND_API_KEY: REPLACE_ME
  RESEND_INBOUND_SIGNING_SECRET: REPLACE_ME
  ANTHROPIC_API_KEY: REPLACE_ME
  CRON_SECRET: REPLACE_ME
  PRESSFARM_API_KEY: REPLACE_ME
  NEXT_PUBLIC_APP_URL: https://pressfarm.io
