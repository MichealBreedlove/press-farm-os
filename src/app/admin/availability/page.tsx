/**
 * /admin/availability — Availability dashboard
 *
 * Shows upcoming delivery dates with availability status.
 * Admin selects a date to edit availability.
 *
 * TODO: Build availability dashboard
 */
export default function AdminAvailabilityPage() {
  return (
    <main>
      <header className="bg-white border-b border-gray-100 px-4 py-4">
        <h1 className="text-lg font-semibold">Availability</h1>
      </header>

      <div className="px-4 py-6">
        {/* TODO: List of upcoming delivery dates with availability status */}
        <p className="text-center text-gray-400 text-sm">
          Availability dashboard — coming in Phase 1 build
        </p>
      </div>
    </main>
  );
}
