/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable PWA in future via next-pwa
  // Strict mode for better React development
  reactStrictMode: true,
};

module.exports = nextConfig;
